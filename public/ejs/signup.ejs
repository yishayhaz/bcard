<!DOCTYPE html>
<html>
<head>
    <%- include('../partials/top') %>
    <link rel="stylesheet" href="css/signup.css">
    <meta name="google" content="nositelinkssearchbox" />
    <title>D&M Bcard - Sign Up</title>
</head>
<body>
    <div class="container">
        <div class="goback">
            <i onclick="location.href = '/'" class="fas fa-arrow-circle-right"></i>
        </div>
        <h2>התחברות \ הרשמה</h2>
        <div class="content" >
            <div>
                <form action="signup" method="POST" data-aos="<%= inp_email != 'undefined' ? '' : 'fade-up' %>" data-aos-delay="<%= inp_email != 'undefined' ? '' : '500' %>">
                    <label>
                        <label for="email">כתובת אימייל:</label>
                        <input type="email" name="email" placeholder="כתובת האימייל"
                            value="<%= inp_email != 'undefined' ? inp_email : '' %>"
                        >
                        <span class="err">
                            <% if(typeof(EmailErr) != 'undefined') { %> 
                                <%= EmailErr %>
                            <% } %>
                        </span>
                    </label>
                    <label>
                        <label for="email">סיסמה:</label>
                        <input type="password" name="password" placeholder="הכנס סיסמא">
                        <span class="err">
                            <% if(typeof(PassErr) != 'undefined') { %> 
                                <%= PassErr %>
                            <% } %>
                        </span>
                    </label>
                    <button type="submit">אישור</button>
                    <% if(typeof(PassErr) != 'undefined') { %>
                        <span class="forgot_password" href="#">
                            שכחת סיסמא?
                            <a href="https://api.whatsapp.com/send?phone=+972546144870&text=היי, שכחתי את הסיסמא לחשבון שלי, תוכל לעזור לי?",target="blank">צור קשר לאיפוס סיסמא &#8592;</a>
                        </span>
                    <% } %>
                </form>
            </div>
            <div class="svg" data-aos="<%= inp_email != 'undefined' ? '' : 'fade-left' %>">
                <img src="imgs/signup.svg" />
            </div>
        </div>
    </div>
 <%- include('../partials/bottom') %>
